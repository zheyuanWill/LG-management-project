# LG 船舶修理管理系统 — 功能总览与流程详解

> 生成时间：2026-08-14　|　系统状态：9 个容器全部健康运行
> 技术栈：FastAPI + SQLAlchemy(async) + PostgreSQL(pgvector) + Redis + Celery + MinIO + AI 服务；前端 Vite + React + TanStack Router/Query（Caddy 静态托管）

---

## 一、服务拓扑（已全部启动并验证）

| 容器 | 服务 | 端口 | 状态 |
|---|---|---|---|
| lg-postgres | PostgreSQL + pgvector | 5432 | healthy |
| lg-redis | Redis（缓存 + Celery broker） | 6379 | healthy |
| lg-minio | MinIO 对象存储（桶 `lg-erp`） | 9000/9001 | healthy |
| lg-backend | 后端 API（FastAPI） | 8000 | healthy |
| lg-ai-service | AI 推理服务（chat/embed/OCR） | 8001 | up |
| lg-celery | Celery Worker | — | up（pong 在线） |
| lg-celery-beat | Celery Beat 定时调度 | — | up |
| lg-frontend | 前端（Caddy 静态服务） | 3000→80 | up |
| lg-caddy | Caddy 反向代理（/api/* → backend） | 80/443 | up |

访问入口：前端 `http://localhost:3000`，Caddy 统一入口 `http://localhost`（前端 + `/api/*` 反代后端），API 文档 `http://localhost:8000/docs`。

---

## 二、功能模块总览

系统围绕「船舶修理」业务，按 **项目类型** 分为四条主线，外加通用的客户、文件、知识库、随手存、风险、报表能力：

| # | 模块 | 业务含义 | 前端入口 |
|---|---|---|---|
| 1 | 认证 Auth | 登录 / 身份 | `/login` |
| 2 | 客户 Customers | 船东主数据 | `/customers` |
| 3 | 监修 Supervision | 修船现场进度管理 | `/supervision` |
| 4 | 买卖经纪 Brokerage-Sale | 船舶买卖经纪 | `/brokerage-sale` |
| 5 | 修船经纪 Brokerage-Repair | 修船业务经纪 | `/brokerage-repair` |
| 6 | 备件供应 Spare-Parts | 备件订购 + 物流追踪 | `/spare-parts` |
| 7 | 任务 Tasks | 监修任务 + 日报上传 | 监修详情内 |
| 8 | 报表 Reports | 日报 / 周报（AI 辅助） | 监修详情内 |
| 9 | 风险 Risks | 风险登记 + 规则检测 | 监修详情内 |
| 10 | 随手存 Quick-Save | 现场随手记 + AI 联想项目 | `/quick-save` |
| 11 | 知识库 Knowledge | RAG 文档问答 | `/knowledge` |
| 12 | 文件中心 Files | 通用文档库 | `/files` |

---

## 三、各功能详细流程

### 1. 认证 Auth
**目的**：多角色 ERP 的统一登录与身份识别（角色：`user` / `admin` / `disabled`）。

流程：
1. `POST /auth/login`：查 `User` → bcrypt 校验密码 → 拒绝 `disabled` 角色 → 返回 JWT（`{sub, role}`，HS256，有效期 1440 分钟）。
2. 前端 `useAuthStore` 保存 `access_token`+`user`，axios 拦截器在请求头挂 `Authorization: Bearer <token>`；401 自动登出跳 `/login`。
3. `GET /auth/me`：返回当前用户资料。
4. `POST /auth/register`：**仅 admin 角色**可调用，拒绝重名，bcrypt 加密后建用户（这是目前唯一的「用户管理」入口）。
> ⚠️ 后端 `GET/POST /users` 是空 stub，真正的用户管理只能走 register。

---

### 2. 客户 Customers（船东主数据）
**目的**：维护船东/委托方档案，供经纪与修船业务引用（含 `survey_conclusion` 调研结论字段）。

流程：`GET/POST /customers` + `GET/PATCH/DELETE /customers/{id}`。列表支持按名称模糊搜索 + 分页。无 AI/MinIO 调用。前端 `customers.tsx` 提供搜索表格与 `CustomerForm`，并在 `new-project.tsx` 内联新建船东。

---

### 3. 监修 Supervision（修船现场主线）
**目的**：管理一艘船的进厂修理全过程。项目类型 `supervision`，编号规则 `LG-SV-{year}-{seq:03d}`。

核心流程：
- 新建项目 `POST /projects` → `generate_project_number` 生成编号，状态 `active`。
- 项目详情 `GET /projects/{id}` 经 `_enrich_project` 富化：联表船东、未解决风险摘要、是否有待确认报表（红点标记）。
- `GET /projects/{id}/stats`：任务数、完成率、未解决风险数。
- **关键状态流转 — 移交监修**：`POST /projects/{id}/handover-to-supervision` 仅对 `brokerage_repair` 类型生效 → 将项目 `type` 改为 `supervision`、`status` 改为 `active`，并**自动播种 5 条默认任务**（进厂检验…出厂交付）。
- 删除 `DELETE /projects/{id}` 为**软删除**（状态置 `cancelled`）。

前端：`/supervision` 列表 → `/supervision/$id` 详情页（系统核心），含头部信息卡 + `RiskTicker` 风险横幅 + 5 个懒加载标签页：**任务管理 / 日报 / 周报 / 风险历史 / 完工资料**。

---

### 4. 买卖经纪 Brokerage-Sale
**目的**：船舶买卖经纪全生命周期，每项目固定四张子表（每个子资源一个项目仅一条）。

流程（均在 `/brokerage/projects/{pid}/...` 下）：
- **背景调研 Survey**：`GET/POST/PATCH/DELETE /surveys` — 记录买卖背景。
- **商务结果 Commercial**：`GET/POST/PATCH /commercials` — 报价 / 佣金 / 收款状态。
- **MOA 合同 Contract**：`GET/POST /contracts` — **上传 MOA 文件到 MinIO**（返回 `moa_file_key`）。
- 前端 `/brokerage-sale/$id` 标签页：项目信息 / 背景调研 / 商务结果 / MOA 合同。

---

### 5. 修船经纪 Brokerage-Repair
**目的**：修船业务经纪，比买卖经纪多一个「修船经纪主表 + 移交」环节。

流程：
- **修船经纪主表 RepairBrokerage**：`GET/POST/PATCH /repair-brokerage` — 修船业务主信息；`contract_file_key` 通常由 `/files/url` 传入（即先传文件再关联 key）。
- **调研/商务**：同模块 4 的 surveys / commercials。
- **关键状态流转 — 经纪移交**：`POST /repair-brokerage/{rid}/handover` → 置 `handed_over=True`（带幂等保护，重复调用安全）。移交后项目可走模块 3 的「移交监修」播种任务。
- 前端 `/brokerage-repair/$id` 标签页：项目信息 / 商务结果 / 修船合同 / 移交。

> 设计注意：8 个经纪/修船子资源在「尚未创建」时返回 404（REST 正确但 UX 差），建议改 200+null 或统一 `/info` 接口（已在 `api_field_audit.md` 标注为 P1 设计缺口）。

---

### 6. 备件供应 Spare-Parts
**目的**：备件订购 + 全链路物流追踪，含香港签收与发票。

流程：
- **备件明细 SparePartDetail**：`GET/POST /spare-parts` + `PATCH/DELETE /spare-parts/{sid}` — 名称/型号/数量。
- **备件照片 SparePartPhoto**：仅 logo / loading 两类（`/spare-photos`），存 MinIO。
- **物流时间线 Logistics**：固定 **8 个节点**（`LOGISTICS_NODE_ORDER`）。`LogisticsNode` 含 `completed` 布尔 + `attachments` JSON。
  - **级联完成规则**（核心业务规则）：完成节点 N → 把 N 及**之前所有**节点标记完成；重开节点 N → 把 N 及**之后所有**节点标记未完成。
  - 节点级 `complete / reopen` 端点 + 备件级物流端点均可用。
- **HK 签收 HkSignature** / **发票 Invoice**（默认用途「用于出口退税」）：每项目一条。
- 前端 `/spare-parts/$id`：备件选择器 + 2 标签页（备件信息 / 物流时间线）。物流时间线每个节点可上传文件（发货照 / 香港签收 / 发票 / 结算）。
> ⚠️ 后端独立组件 `HKSignature.tsx` / `InvoiceForm.tsx` / `SparePhotos.tsx` 是**死代码**（未被引用），签收/发票实际在 `LogisticsTimeline` 节点内处理。

---

### 7. 任务 Tasks（监修进度）
**目的**：监修现场的任务拆分与每日进度上报 + 照片留痕。

流程：
- 任务 CRUD：`GET/POST /tasks/projects/{pid}/tasks`（新建时 `sort_order = max+1`）+ `PATCH/DELETE /tasks/tasks/{id}`。
- 每日更新：`GET/POST /tasks/{tid}/daily-updates` — 记录日期 / 状态 / 备注 / 语音。
- **照片上限规则**：`POST /daily-updates/{uid}/photos` → `photo_service` 强制 **每任务每天最多 2 张**照片，超限抛 `PermissionError`，通过则传 MinIO。删除 `DELETE /tasks/task-photos/{pid}`。
- 前端：`TaskList`（建任务）、`TaskItem`（改/删）、`DailyUpdateForm`（日报更新）、`PhotoUploader`（受 2 张限制）。

---

### 8. 报表 Reports（日报 / 周报，AI 辅助、人在回路）
**目的**：基于现场数据 + 知识库，AI 生成日报/周报草稿，人工确认后定稿。

**日报流程（human-in-the-loop）**：
1. `POST .../daily-reports/propose`（**同步**）：`propose_daily_report` 用确定性逻辑汇总当天工作（来自 `TaskDailyUpdate`）+ 项目小结（来自任务与近 14 天日报）；随后 **两个并行 AI 调用**（`asyncio.gather`）生成「明日计划候选」+「项目级风险」，均用知识库 RAG `retrieve` 做事实支撑 → 返回候选供前端确认。
2. `POST .../daily-reports/finalize`：写入 `DailyReport`（`confirmed=True`，明日项取用户确认值）。
3. `PATCH .../daily-reports/{id}/confirm`：切换确认态。
4. 另有 `POST .../daily-reports/generate`（**无 HITL**，触发 Celery 自动确认候选，返回 `task_id`，前端轮询 `latest`）。

**周报流程**：`POST .../weekly-reports/generate` → Celery `generate_weekly_report_task`，聚合**已确认**日报，调 `ai_client.chat` 生成总结 + 下周计划；`latest` / `confirm` 同类。

> ⚠️ 前端周报目前为**只读展示**，生成 UI 未接通（代码里无 generate 按钮）。日报编辑器 `daily.$date.tsx` 是接通的。

---

### 9. 风险 Risks（规则引擎 + 登记）
**目的**：项目风险登记与自动检测，红点提示。

流程：
- `GET /risks/summary`：未解决风险列表（dashboard 横幅用）。
- `GET /risks/projects/{pid}/risks`：可按 `resolved` 过滤。
- **自动检测** `POST /risks/projects/{pid}/risks/ai-detect` → Celery `detect_risks_task` → `detect_risks` 是**确定性阈值规则**（非 LLM）：
  - 未开始任务 >50% → warning；
  - 进行中任务超过 `planned_end_date` → critical；
  - 项目进行中但完成率 <30% → info。
  - 去重：跳过标题已存在且未解决的条目。
- `PATCH /risks/{id}`：标记 resolved / 改 detail / risk_level。
> 重要澄清：所谓「AI 自动检测」实为**规则引擎**，不含 LLM / 知识库 / 日报语义分析 —— 前端标签建议改为「自动进度检查」以免误导。

---

### 10. 随手存 Quick-Save（现场随手记 + AI 联想）
**目的**：现场快速记录文字/图片，AI 联想关联项目。

后端流程（真实）：
- `POST /quick-saves/text` → `recognize_content` 调 `ai_client.chat`（修船信息抽取 prompt）→ 存 `recognized_text`；再 `suggest_projects` 按关键词（船名/项目号/备注）匹配**活跃**项目打分。
- `POST /quick-saves/image` → 传 MinIO（png）；**OCR 当前关闭**，故 `recognized_text` 为空。
- `PATCH /quick-saves/{id}` → `confirm_save` 置 `confirmed_project_id` + 状态 `confirmed`。
- 模型 `QuickSave`：text/image 两类，双项目外键（建议/确认），状态 pending/confirmed/deleted。

> ⚠️ **前端 `quick-save.tsx` 完全 mock**：`handleRecognize` 用 `setTimeout(1000)` 返回**硬编码假项目**，从不发请求、不调识别 API；确认只是 `alert(...)`。可复用的 `SuggestionList` 组件未被使用。这是当前最明显的数据接线缺陷，需接通真实后端。

---

### 11. 知识库 Knowledge（RAG 向量问答）
**目的**：修船知识文档入库 + 向量检索问答。

流程：
- `POST /knowledge/documents`：校验扩展名（pdf/doc/docx/txt/md）→ 传 MinIO → 建 `KnowledgeDocument`（`embedding` 暂空）→ 触发 Celery `ingest_document_task`。
- **入库任务**：从 MinIO 重新下载 → 抽取文本（PyPDF2 / python-docx / utf-8）→ 切块（500 字 / 50 重叠）→ 批量 `ai_client.embed`（BAAI/bge-small，512 维）→ 存 `KnowledgeEmbedding`（pgvector `VECTOR(512)`，删除文档时级联删向量）。
- `POST /knowledge/query`（同步 RAG）：embed 查询 → pgvector `cosine_distance` 检索 → 重排引用 → `ai_client.chat` 生成答案。
- `POST /knowledge/query/stream`（SSE 流式）：先发 `event: citations` 引用，再 `data:` 增量返回（`ai_client.chat_stream`）。
- 前端 `/knowledge`：`DocUploader` 上传 + 分类（QM/SUP/REP/BRO/SPL/SPEC）；`QAChat` 调流式 `/query/stream`（原生 fetch SSE），聊天记录存 **localStorage（仅本地，不落后端）**。

---

### 12. 文件中心 Files（通用文档库）
**目的**：任意文档的统一存取与预览。

流程：
- `POST /files/upload`：读字节 → 按文件名关键词 `detect_file_type`（合同/收据/照片等）→ 存 MinIO + 建 `File` 记录（`uploaded_by`）。
- `GET /files`、`GET /files/{id}/download`、`GET /files/{id}/preview`：返回 **1 小时有效 presigned URL**（host 重写为 `MINIO_PUBLIC_ENDPOINT` 让浏览器可达）。
- **关键**：`POST/GET /files/url` 按 `storage_key` **直接解析** presigned URL，覆盖不在 `files` 表里的对象（MOA、物流、证书、知识库文档）—— 单一入口服务所有 MinIO key。
- `DELETE /files/{id}`：MinIO 对象 + DB 行一并删。
> ✅ 已复核：`FileUploadResponse`（`schemas/file.py:27`）已含 `id: int`，`routers/files.py:51` 也返回 `id` —— 上传后前端可正常引用文件，原 P0 缺陷已不存在（属误报）。

---

## 四、跨模块共用基础设施
- **鉴权**：所有非 auth 端点 `Depends(get_current_user)` 解码 JWT 重载 `User`；`require_role()` 仅 register 使用。
- **MinIO**：桶 `lg-erp`，key 格式 `YYYYMMDD_HHMMSS_<uuid>.<ext>`，presigned 1h。
- **AI 服务**：`ai_client` 调 `AI_SERVICE_URL` 的 `/v1/chat`、`/v1/embed`、`/v1/chat/stream`、`/v1/qa`（httpx 60s）。OCR 默认关闭。
- **Celery**：Redis broker/backend；任务经 `run_async`（持久 per-thread 事件循环，避免 `asyncio.run` 关闭循环破坏 SQLAlchemy 连接池）桥接异步 DB。所有任务出错返回 `{"status":"failed"}` 而非抛异常，需调用方另行轮询状态。

---

## 五、数据接线 / 实现缺口复核结果（2026-08-14 逐条读代码核对）

> 以下为本文档初版列出的缺口，经**逐条阅读源码**后的真实结论与处理。

| 初判 | 复核结论 | 处理 |
|---|---|---|
| P0 `files/upload` 响应缺 `id` | **误报** —— `FileUploadResponse` 已有 `id`，router 也返回 | 不修 |
| P2 `/auth/me` 无 `phone` 且模型无 phone | **误报** —— `User.phone` 字段存在，`/auth/me` 已返回 `phone` | 不修 |
| P0（前端）`/quick-save` 整页 mock | **真实** | ✅ 已修：`routes/quick-save.tsx` 接通 `POST /quick-saves/text\|image`，映射到既有 `SuggestionList`（确认 PATCH `confirmed_project_id` / 删除 PATCH `status=deleted`） |
| P1 周报 `generate` UI 未接通 | **真实** | ✅ 已修：新增 `WeeklyReportList.tsx`（生成按钮 + 轮询 `.../weekly-reports/latest` → invalidate 刷新），接入 `ProjectTabs` 周报 tab |
| P1 `/users` 空 stub | **真实** | ✅ 已修：`routers/users.py` 改为真实 `GET`（列表）+ `POST`（admin 建用户，复用 `hash_password`+重名校验）；`UserCreate` 增加可选 `phone` |
| 标签误导：风险「AI 自动检测」 | **真实**（实为规则引擎） | ✅ 已修：`RiskList` 按钮改为「自动进度检查」 |
| 死代码：`HKSignature/InvoiceForm/SparePhotos` | **真实**（从未被引用） | ✅ 已删（功能已由 `LogisticsTimeline` 节点覆盖） |
| 死代码：`lib/websocket.ts` 从未 connect | **真实**但后端无 Socket.IO 服务器 | ⏸️ 未实现：属独立大功能（需 ASGI WS 服务 + 事件推送 + 鉴权），建议另立专项 |
| P1（设计）经纪/修船子资源未创建返回 404 | 设计性 UX 缺口 | 保留建议（改 200+null 或统一 `/info`），本轮未改 |
| P2 知识库聊天记录仅 localStorage | 真实（跨设备不同步） | 保留，本轮未改 |

> 说明：前端 `tsc --noEmit` 存在大量**历史**类型错误（number→string、`asChild` 等），但 `build.js` 仅执行 `vite build`（esbuild 不去类型），dist 生成即视为成功，故不影响构建；本轮改动未引入新错误。
> 全部代码修复后已执行 `docker compose up -d --build` 全量重建。
