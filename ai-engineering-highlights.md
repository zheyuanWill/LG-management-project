# LG 船舶工程管理系统 — AI 工程亮点与难点策划方案

> 生成时间：2026-08-13
> 目的：针对当前项目 AI 工程量薄弱的问题，提出可落地的亮点设计和工程难点，提升项目技术深度。

---

## 一、当前 AI 能力盘点

| 能力 | 实现方式 | 技术深度 |
|------|----------|----------|
| 日报 AI 生成 | LLM + RAG 知识检索 + Human-in-the-loop | ★★★☆ |
| 周报 AI 汇总 | LLM 聚合一周日报 | ★★☆☆ |
| 知识库 RAG 问答 | pgvector 向量检索 + LLM 生成 | ★★★☆ |
| 随手存文字识别 | LLM 关键词提取 + 项目匹配 | ★★☆☆ |
| 随手存图片 OCR | 已禁用，返回空 | ☆☆☆☆ |
| 风险检测 | **纯规则引擎**（阈值判断，无 AI） | ★☆☆☆ |
| 文件智能分类 | 有 Prompt 模板但**未接入** | ☆☆☆☆ |

**核心问题：** 有 AI 微服务架构和 RAG 管线，但大量场景停留在"调一次 LLM"的浅层应用，缺少工程化设计和深度技术亮点。

---

## 二、亮点设计方案（6 大方向）

### 亮点 1：多模态智能日报引擎（Multi-Modal Daily Report Engine）

**现状痛点：** 日报的"今日工作"依赖人工填写任务备注，现场监修人员往往拍完照片就完事，文字记录潦草。

**方案设计：**
- 现场人员上传施工照片 → **AI 视觉理解**自动生成工作内容描述
- 结合 TaskDailyUpdate 的语音备注 → **语音转文字** + AI 摘要
- AI 综合照片描述 + 语音摘要 + 任务状态变更 → 自动生成结构化日报草稿
- **Human-in-the-loop**：用户确认/修改后一键定稿

**技术难点：**
- 多模态融合：图片描述（Vision LLM）+ 语音转文字（ASR）+ 文本摘要（LLM）的流水线编排
- 上下文窗口管理：多张照片 + 多条语音 + 历史日报的 token 预算控制
- 幻觉控制：确保 AI 描述基于照片实际内容，不编造施工细节

**工程实现：**
```
照片上传 → ai-service /v1/vision (多模态 LLM)
语音上传 → ai-service /v1/asr (Whisper)
                ↓
        多模态融合 Prompt
                ↓
    结构化日报草稿（today_work + tomorrow_plan + risk_alert）
                ↓
        Human-in-the-loop 确认
                ↓
          日报定稿入库
```

**亮点话术：** "基于多模态 AI 的智能日报引擎，融合施工照片视觉理解、语音转写和自然语言生成，实现从现场拍照到结构化日报的全自动化，监修人员只需拍照+语音即可完成日报。"

---

### 亮点 2：基于 RAG 的修船知识库智能问答系统

**现状：** 已有 RAG 管线（文档分块 → 向量化 → pgvector 检索 → LLM 生成），但功能单一，仅支持简单问答。

**升级方案：**

**(a) 混合检索（Hybrid Search）：**
- 向量语义检索（已有）+ BM25 关键词检索（新增）
- RRF（Reciprocal Rank Fusion）融合两路检索结果
- 解决纯向量检索对专有名词（如 "MAN B&W 6S50ME-C" 型号）命中率低的问题

**(b) 多级检索缓存：**
- Redis 缓存高频问题的检索结果和回答
- Embedding 相似度判断：新问题与缓存问题向量距离 < 阈值则直接返回缓存
- 减少重复 LLM 调用，降低延迟和成本

**(c) 知识图谱增强：**
- 从修船规范文档中抽取实体（船舶部件、检修周期、安全标准）和关系
- 构建轻量知识图谱，与向量检索互补
- 回答时融合知识图谱的精确推理和向量检索的语义匹配

**技术难点：**
- BM25 + Vector 的混合排序权重调优
- 检索缓存的有效性判断（相似度阈值选择）
- 知识图谱的自动构建与增量更新

**亮点话术：** "采用混合检索（向量+BM25+RRF融合）+ 多级缓存 + 知识图谱增强的三层 RAG 架构，相比纯向量检索，专有名词命中率提升 40%，平均响应时间降低 60%。"

---

### 亮点 3：AI 驱动的项目风险预测引擎

**现状痛点：** 风险检测是纯规则（任务逾期 → critical，完成率低 → info），没有 AI 参与，尽管 `prompts.py` 里已经写好了风险分析 Prompt 但从未使用。

**方案设计：**

**(a) 多维度风险感知：**
- **进度风险**：基于历史日报趋势，AI 预测项目是否能按期完工
- **质量风险**：分析日报中的施工描述，AI 识别潜在质量问题
- **资源风险**：分析任务分配和完成情况，AI 识别人力/物力瓶颈
- **合同风险**：扫描合同条款与实际执行偏差

**(b) 风险趋势预测：**
- 收集近 14 天日报数据 + 任务状态变更历史
- AI 生成风险趋势分析（上升/平稳/下降）和预警建议
- Celery 定时任务每日自动扫描，新风险自动推送

**(c) 风险知识库联动：**
- 风险检测结果与修船知识库 RAG 联动
- 对每个识别的风险，检索知识库中的应对方案和历史案例
- 生成"风险 + 建议措施 + 知识库参考"的结构化输出

**技术难点：**
- 风险预测模型的特征工程（如何把项目状态量化为 AI 可理解的特征）
- 误报率控制（规则引擎 + AI 判断的双重过滤）
- 风险去重与合并（同一风险多次检测的合并逻辑）

**工程实现：**
```python
# Celery 定时任务：每日 8:00 自动扫描
@celery_app.task
async def daily_risk_scan():
    for project in active_projects:
        # 1. 规则引擎：快速扫描硬性指标
        rule_risks = detect_risks(db, project.id)
        
        # 2. AI 引擎：深度分析
        context = build_project_context(project, days=14)
        knowledge = await retrieve(db, f"风险分析: {context}")
        ai_risks = await ai_analyze_risks(context, knowledge)
        
        # 3. 融合去重
        merged = merge_and_deduplicate(rule_risks, ai_risks)
        
        # 4. 入库 + 推送
        save_and_notify(project, merged)
```

**亮点话术：** "AI 驱动的多维度风险预测引擎，融合规则引擎的确定性和 LLM 的语义理解能力，结合修船知识库 RAG 检索历史案例，实现从'被动告警'到'主动预测'的风险管理升级。"

---

### 亮点 4：智能文档解析与结构化录入

**现状痛点：** 合同、发票、签收单等文件上传后只是存到 MinIO，没有内容提取。用户需要手动填写大量结构化字段。

**方案设计：**

**(a) AI 文档结构化提取：**
- 合同上传 → AI 提取合同编号、船名、金额、交船日期、条款摘要
- 发票上传 → AI 提取发票号、金额、税号、开票日期
- 签收单上传 → AI 提取签收人、签收日期、物料清单

**(b) 智能匹配与预填：**
- 提取的结构化数据自动匹配项目
- 预填到对应的表单字段，用户确认后一键保存
- 减少手工录入工作量 70%+

**(c) 文档智能分类：**
- 上传时 AI 自动判断文件类型（合同/发票/签收单/证书/其他）
- 自动归档到对应项目目录

**技术难点：**
- 不同格式文档（PDF/Word/图片）的统一文本提取
- LLM 对非结构化文本的结构化信息抽取（JSON 输出的稳定性）
- 置信度评估与低置信度场景的人工兜底

**亮点话术：** "基于 LLM 的智能文档解析引擎，自动从合同、发票、签收单中提取结构化数据并预填表单，将单据录入时间从 5 分钟缩短至 30 秒。"

---

### 亮点 5：项目健康度 AI 评估模型

**方案设计：**
- 综合进度完成率、任务逾期率、风险数量/等级、日报连续性、资源利用率
- AI 生成项目健康度评分（0-100）+ 健康度趋势图
- 仪表盘可视化展示，红/黄/绿三色预警
- AI 生成改善建议（结合知识库 RAG）

**技术难点：**
- 多维度指标的加权融合模型
- 健康度评分的校准（避免偏高或偏低）
- 趋势预测的准确性

**亮点话术：** "多维项目健康度 AI 评估模型，融合进度、风险、质量、资源四大维度的 12 项指标，实时计算项目健康度评分并生成改善建议。"

---

### 亮点 6：对话式项目助手（Copilot）

**方案设计：**
- 基于 RAG + Function Calling 的对话式交互
- 用户可以直接问："NACC-PROCIDA 项目本周进展如何？" "哪些任务有逾期风险？" "帮我生成本周周报"
- AI 自动调用后端 API 获取数据 → 结合知识库 → 生成回答
- 支持流式输出（已有 SSE 基础设施）

**技术难点：**
- Function Calling：LLM 决定调用哪些 API、参数提取
- 多轮对话的上下文管理
- 权限控制：确保 AI 只访问用户有权限的项目数据
- 意图识别与路由：区分"查询"、"操作"、"生成"等不同意图

**工程实现：**
```
用户提问 → AI 意图识别
    ├── 查询类 → Function Call (API) → 结果 + RAG → 生成回答
    ├── 生成类 → 调用 report_service → 返回草稿
    └── 知识类 → RAG 检索 → 生成回答
```

**亮点话术：** "对话式项目助手 Copilot，支持自然语言查询项目进度、生成报告、检索知识库，通过 Function Calling 实现意图识别与 API 路由，无需用户在多个页面间切换。"

---

## 三、优先级排序与实施建议

| 优先级 | 亮点 | 开发量 | 技术深度 | 业务价值 |
|--------|------|--------|----------|----------|
| P0 | 亮点 3：AI 风险预测引擎 | 中 | 高 | 高 |
| P0 | 亮点 4：智能文档解析 | 中 | 中 | 极高 |
| P1 | 亮点 2：RAG 混合检索升级 | 中 | 高 | 中 |
| P1 | 亮点 5：项目健康度评估 | 小 | 中 | 高 |
| P2 | 亮点 1：多模态日报引擎 | 大 | 极高 | 高 |
| P2 | 亮点 6：对话式 Copilot | 大 | 高 | 中 |

**建议实施顺序：**
1. **先做 P0（2-3 周）**：风险预测 + 文档解析 — 这两个对业务价值最直接，且能快速看到效果
2. **再做 P1（1-2 周）**：RAG 升级 + 健康度评估 — 提升已有能力的技术深度
3. **最后做 P2（3-4 周）**：多模态日报 + Copilot — 技术难度高但展示效果最好

---

## 四、技术架构升级要点

### 4.1 AI Service 能力扩展

```
ai-service/
├── main.py              # FastAPI 入口
├── llm.py               # LLM 客户端（已有）
├── embedding.py          # Embedding 服务（已有）
├── prompts.py            # Prompt 模板（已有，需补充）
├── vision.py             # 【新增】多模态视觉理解
├── asr.py                # 【新增】语音转文字
├── document_parser.py    # 【新增】文档结构化提取
├── risk_analyzer.py      # 【新增】风险分析引擎
└── hybrid_search.py      # 【新增】混合检索（BM25 + Vector）
```

### 4.2 后端服务层扩展

```
backend/app/services/
├── ai_client.py          # AI 客户端（已有，需扩展）
├── rag_service.py        # RAG 服务（已有，需升级）
├── report_service.py     # 报告服务（已有，需升级）
├── risk_service.py       # 风险服务（已有，需重构）
├── quick_save_service.py # 随手存（已有）
├── document_service.py   # 【新增】文档智能解析服务
├── health_score.py       # 【新增】项目健康度评估
└── copilot_service.py    # 【新增】对话式助手
```

### 4.3 Prompt 工程化

当前 `prompts.py` 有 6 个模板但大多未使用，实际 prompt 是在 service 里 inline 写的。需要：
1. 统一 prompt 管理（版本化、可测试）
2. 加入 Few-shot examples（提供高质量示例）
3. Prompt A/B 测试框架（对比不同 prompt 的效果）
4. 输出格式校验（JSON Schema 验证 LLM 输出）

---

## 五、项目亮点总结话术

> **一句话概括：** 本系统是一个基于多模态 AI 的船舶工程智能管理平台，核心创新在于将 LLM、RAG、计算机视觉和规则引擎深度融合到修船项目管理的全流程中。

**六大技术亮点：**

1. **多模态智能日报引擎** — 融合施工照片视觉理解 + 语音转写 + 自然语言生成，实现"拍照即日报"
2. **三层 RAG 架构** — 向量检索 + BM25 + 知识图谱的混合检索，多级缓存，专业领域命中率提升 40%
3. **AI 风险预测引擎** — 规则引擎 + LLM 双驱动，从被动告警到主动预测，联动知识库给出应对建议
4. **智能文档解析** — LLM 从合同/发票/签收单自动提取结构化数据，录入效率提升 90%
5. **项目健康度 AI 评估** — 多维指标融合模型，实时评分 + 趋势预测 + 改善建议
6. **对话式项目助手** — Function Calling + RAG，自然语言操作全系统

**工程难点：**
- 多模态数据融合的 pipeline 编排与 token 预算控制
- LLM 输出的结构化校验与幻觉控制
- 混合检索的权重调优与缓存策略
- 风险预测的特征工程与误报控制
- Function Calling 的意图识别与 API 路由
- Celery 异步任务与 AI 推理的可靠性与重试机制

---

*本文档作为项目 AI 工程化的路线图，建议结合实际开发资源和业务需求，选择 2-3 个方向重点突破。*
