# 前后端修复建议（保持现有 UI 风格）

> 结论先行：**不要重写 UI，要"修接线"**。你喜欢的视觉壳子（布局/组件/风格）不是问题，问题在 UI 下面的数据流——表单收集了数据却发错端点/字段名，导致 422/404 或静默丢字段。保留视觉，重做数据层。

---

## 一、前端：保留风格，修接线（P0，机械活但量大）

### 1. 统一 API 层 & 修正字段/路径错位
逐条都是"前端发的 ≠ 后端要的"（实测 422/404）：
- 任务创建：`title` → `name`（否则 422）
- 日报确认：路径错位 + `risk_reminders` → `risk_alert`（备注/风险提醒丢失）
- 知识库问答：`/knowledge/ask` → `/knowledge/query`，body `question` → `query`（否则 404，界面永远问不出东西）
- 客户：`survey_conclusion` 从不发；`email` 后端无此列 → 改存 `phone` 或后端加列
- 发票：`tax_no` → `tax_number`（税号永不保存）
- 调研：`details` → `survey_detail`（详情永不存也不显示）
- 修船移交：路由写错 → 404
- 物流节点：`type/date` → `node_type/node_date`，且枚举值写错 → 422

### 2. 删掉假功能 / 硬编码
- 随手存整套 mock（`setTimeout` + 写死"远洋一号"等假数据 + `alert` 确认）→ 接真实后端
- `RiskDetail` 用裸 `fetch` 绕过统一 client → 走统一封装
- 备件客户下拉写死字符串 id（后端是整数）→ 用真实整数 id
- 物流节点枚举值与后端不一致 → 对齐

### 3. 补"该显示却没显示"的字段
后端有、前端从不收集/展示：`customer` 结论/备注、`RiskEvent.title/detail/risk_level`、周报 `progress_total/key_events`、合同 `name/url`、`Task.note/photos`（后端 `Task` 甚至还没这俩字段，需后端也补）。

---

## 二、后端：契约清晰化（P1）

- **项目列表/详情补聚合字段**：前端卡片直接要 `customerName`/`customerPhone`、`riskSummary`、红点 `hasUnconfirmedReport`，现在后端都不返回。建议新增"统一项目视图"聚合端点，而不是前端逐卡片再拉详情（慢、且会刷屏）。
- **8 个子资源"无数据就 404"** → 改成 `200 + null`（调研/商务/合同/修船经纪/备件/物流/签收/发票首跑必 404，纯属噪音）。
- **旧文档无向量**：embedding 禁用期入库的文档没有向量，无法被检索 → 加一次重索引（或上传时强制重算 embedding）。
- **异步生成不轮询**：周报/日报异步生成，前端不轮询最终态 → 永远空白。后端补 `GET .../daily-reports/latest`、`.../weekly-reports/latest` 让前端拉最终态。

---

## 三、AI 功能：诚实化（三个决定）

1. **风险"AI"**：目前是**纯规则引擎**（`risk_service.detect_risks` 数逾期任务、完成率<30% 等硬编码阈值），**没调 LLM、没检索知识库、没读日报备注/图片**。
   → **建议：保留规则引擎，改名"自动进度检查"**，界面标注"非 AI"；路线图再决定是否接知识库+日报做成真 AI。
2. **随手存 AI 识别**：后端是**真 AI**（文字→LLM 抽取；图片→OCR+LLM 抽取；再按关键词匹配活跃项目推荐），但前端是 mock、从没调用。
   → **建议：保留并真正接上**——老板随手拍张图就能自动归类，是这个产品的差异化点。OCR 失败的图片降级为纯文本/手动选择。
3. **知识库 Q&A**：后端**能用**（实测返回带引用的真实答案）。前端修好端点即可，无需大改。

---

## 四、关于 `docker compose up -d`

- **前端是 Vite 独立跑**，前端修复不需要 rebuild，hot-reload 即时生效。
- 只有**后端**改动（聚合字段、404→200、重索引）才需要 `docker compose up -d backend`（或 `docker restart lg-backend`）。

---

## 推荐执行顺序

1. **P0 前端接线修复**（立刻消除"数据丢失 / 问不出东西"）—— 最大头、最该先做。
2. **P1 后端聚合字段 + 子资源 200**。
3. **随手存接真实后端**。
4. **风险改名 + 写进路线图**。

---

## 三个待你拍板的点（我的默认推荐已标 ✅）

| 问题 | 我的推荐 | 备选 |
|---|---|---|
| 现前端修还是重写？ | ✅ **修接线（保留 UI）** | 从零重写 |
| 风险"AI"怎么处理？ | ✅ **保留规则引擎，改名"自动进度检查"** | 真做成知识库+日报 AI |
| 随手存 AI 识别？ | ✅ **保留并真实接上（降级友好）** | 简化（只存+手动选项目）/ 砍掉 |

只要你说"按推荐来"，我就从第 1 步（P0 前端接线）开始。
