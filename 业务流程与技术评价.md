# LG 船舶管理 ERP — 业务流程梳理与技术/业务评价

> 生成时间：2026-08-21　|　基于：仓库文档（`功能与流程总览.md`/`监修模块组件流程.md`/`business_logic_audit.md`/`recommendation.md`/`PROGRESS.md`/`feature_list.json`）+ 本轮实跑核实（容器状态、登录冒烟、compose 源码、运行日志）
> 结论先行：**业务建模贴合修船行业实际，核心链路（监修+日报/周报 HITL+风险扫描+知识库 RAG）设计合理且已端到端跑通；技术选型整体务实，但存在 3 处真实漂移/债务——Celery 未真正下线、前端构建忽略类型检查、`.env` 明文密钥需确认未被提交。**

---

## 一、Docker 服务启动结果（实跑核实）

`docker compose up -d` 已拉起 **9 个容器**，全部 `Up`；后端 `/docs` 返回 200，登录 `admin/admin123` 返回合法 JWT，AI 服务 `/health` 200。

| 容器 | 服务 | 端口 | 状态 | 实测 |
|---|---|---|---|---|
| lg-postgres | PostgreSQL + pgvector | 5432 | healthy | ✅ |
| lg-redis | Redis | 6379 | healthy | ✅ |
| lg-minio | MinIO（`lg-erp` 桶） | 9000/9001 | healthy | ✅ |
| lg-backend | FastAPI 后端 | 8000 | Up(health starting→200) | ✅ 登录 200 |
| lg-ai-service | AI 推理（chat/embed） | 8001 | Up | ✅ /health 200 |
| lg-celery | Celery Worker | — | Up | ⚠️ **仍在消费任务**（见第四节） |
| lg-celery-beat | Celery Beat 定时 | — | Up | ⚠️ 同上 |
| lg-frontend | 前端（Caddy 静态） | 3000→80 | Up | ✅ :3000 200 |
| lg-caddy | Caddy 反代 | 80/443 | Up | ✅ :80 200 |

访问入口：前端 `http://localhost:3000`，统一入口 `http://localhost`（Caddy 反代 `/api/*`），API 文档 `http://localhost:8000/docs`。

**AI 实际状态**：项目根 `.env` 已设 `LLM_PROVIDER=cloud` + `LLM_API_KEY=sk-...`（DeepSeek），故日报/周报/风险扫描/随手存均调用真实云端 LLM；`OCR_ENABLED=false`（图片 OCR 已按重构砍掉）。**注意**：若某人只执行 `docker compose up -d` 而不带此 `.env`，compose 默认 `LLM_PROVIDER=mock`，AI 功能会静默返回 mock 数据——这点要在团队内说清楚。

---

## 二、业务流程梳理

### 2.1 总体业务模型

系统围绕「船舶修理」业务，核心实体是 **项目（Project）**，按 `type` 分四条主线，外加跨模块通用能力。项目有清晰的状态机：

```
brokerage_repair（修船经纪）
   │  POST /repair-brokerage/{rid}/handover   （幂等，置 handed_over=True）
   ▼
brokerage_repair（已移交）
   │  POST /projects/{id}/handover-to-supervision  （仅对 brokerage_repair 生效）
   ▼
supervision（监修，active）+ 自动播种 5 条默认任务（进厂检验→…→出厂交付）
   │  任务推进 / 每日更新 / 日报 / 周报 / 风险扫描
   ▼
active ──(DELETE 软删除)──▶ cancelled
```

买卖经纪（brokerage_sale）、备件供应（spare_parts）是**独立项目类型**，不走上述移交链。

### 2.2 四条业务主线

**① 监修 Supervision（系统核心，项目类型 `supervision`，编号 `LG-SV-{year}-{seq:03d}`）**
- 新建项目 → `_enrich_project` 富化（联表船东、未解决风险摘要、待确认报表红点）。
- 5 个 Tab：**任务管理 / 日报 / 周报 / 风险历史 / 完工资料**。
- 任务：CRUD + 每日更新（`POST /tasks/{id}/daily-updates`，每任务每天上限 2 张照片，存 MinIO，已持久化）。
- 日报（HITL）：`propose`（确定性汇总今日工作 + AI 明日计划候选 + AI 项目级风险，RAG 支撑）→ 人工确认 `finalize` → 可 `confirm`/下载。
- 周报：`generate` 同步调 DeepSeek `chat_json` 出 `{summary, next_week_plan}`，仅统计 `confirmed=True` 日报。
- 风险：`scan`（规则打底 + RAG 检索知识库 + LLM 综合判断，返回 `[{title,detail,risk_level}]`）+ 人工 `resolved`。

**② 买卖经纪 Brokerage-Sale**
- 每项目固定 4 张子表（每子资源仅一条）：背景调研 Survey / 商务结果 Commercial（报价·佣金·收款状态）/ MOA 合同 Contract（上传 MOA 文件到 MinIO 返 `moa_file_key`）。前端 3 个 Tab。

**③ 修船经纪 Brokerage-Repair**
- 比买卖经纪多「修船经纪主表 + 移交」：RepairBrokerage 主表（`contract_file_key` 由 `/files/url` 关联）→ surveys/commercials → `handover` → 可转监修。

**④ 备件供应 Spare-Parts**
- 备件明细（名称/型号/数量）+ 备件照片（logo/loading，存 MinIO）。
- **物流时间线（核心业务规则）**：固定 8 节点，级联完成（完成节点 N → N 及之前全完成；重开 N → N 及之后全未完成）。
- HK 签收 / 发票（默认「用于出口退税」），每项目一条。

### 2.3 跨模块通用能力

- **认证 Auth**：JWT（HS256，1440 分钟），角色 `user/admin/disabled`；唯一用户管理入口 `POST /auth/register`（仅 admin）/`GET|POST /users`。
- **客户 Customers**：船东主数据（含 `survey_conclusion` 调研结论），供经纪/修船引用。
- **文件中心 Files**：统一 presigned URL（1h，host 重写 `MINIO_PUBLIC_ENDPOINT`）；`/files/url` 按 `storage_key` 直接解析，覆盖 MOA/物流/证书/知识库等所有 MinIO key，单一入口。
- **知识库 Knowledge**：上传 PDF/DOCX/MD/TXT → 多格式清洗 + **按章节切分**（chunk 带 `【书名/章/节】` 元数据）→ fastembed `bge-small-zh-v1.5`(512 维) 入 pgvector → 查询返回结构化 citation + AI answer；聊天记录后端持久化（跨设备同步）。
- **随手存 Quick-Save**：现场文字 → LLM structured output 提取字段 → 联想活跃项目 → 确认关联；图片直接存图手选项目（已砍 OCR/VLM）。
- **实时推送**：FastAPI 原生 WebSocket，按 `project_id` 分房间 + token 鉴权，每日更新触发广播。

### 2.4 端到端典型流程（监修生命周期）

见附图（核心监修业务流程）。一句话：船东委托 → 修船经纪移交 → 进厂监修（任务+每日更新+照片）→ AI 辅助日报/周报（人在回路）→ 规则+RAG+LLM 风险扫描 → 完工资料归档。

---

## 三、业务合理性评价

### 优点（做得对的地方）
1. **领域建模准确**：把「修船」拆成监修/买卖经纪/修船经纪/备件四条线，且用「修船经纪→移交→监修」的状态机串起主数据流，符合船东驻厂/修船服务商的真实作业顺序。
2. **AI 用在刀刃上且诚实化**：日报/周报的「AI 生成草稿 + 人工确认」是典型 HITL，避免 AI 直接定稿出错；风险检测从「假 AI（纯规则）」升级为「规则+RAG+LLM 综合判断」并改名 `scan`，命名不再误导；随手存用 LLM 提取结构化字段是真实提效点。
3. **知识库 RAG 带章节元数据**：修船 SOP 按章节切分 + citation 回带书名/章/节，比纯向量召回更适合规范类文档，答案可溯源。
4. **物流级联完成规则**：用一条确定性业务规则保证时间线一致性，减少人工误操作。
5. **通用文件中心单一入口**：所有 MinIO key 走 `/files/url` 统一签名，避免各模块重复实现下载/预览。

### 风险 / 业务缺口
- **角色与权限过粗**：仅 `user/admin/disabled`，无 GM/船东代表/现场监工等细分角色与数据权限；`/auth/me` 缺 `phone` 曾影响 GM 点击呼叫（部分已修，但细粒度 RBAC 仍缺）。
- **经纪/修船子资源早期 404 噪音**：已改为 200+null，UX 修好；但「每子资源仅一条」的强约束在并发/多版本场景下可能不够（如一份合同多次修订）。
- **AI 外部依赖**：日报/周报/风险/随手存全部依赖 DeepSeek API，限流或密钥失效时降级（风险扫描已 try/except 兜底，周报无数据降级 `no_data`），但**无离线兜底 UI 提示**，用户可能误以为"生成失败"。
- **业务规模 vs 系统重量**：9 容器全栈服务于 5–6 人小团队，重量偏高；合理性的关键在"能否减"（见技术评价 Celery 一节）。

---

## 四、技术合理性评价

### 架构与选型优点
- **FastAPI + SQLAlchemy(async) + asyncpg**：I/O 密集型（大量 MinIO/AI/DB 调用）选异步栈正确，吞吐与开发效率兼得。
- **pgvector 复用 PostgreSQL 做向量库**：免去单独维护 Milvus/Weaviate，RAG 与业务数据同库事务一致，对小团队极友好。
- **MinIO 自托管对象存储**：船修现场照片/合同/验收单等敏感文件自托管，不落第三方云，合规与成本合理。
- **Caddy 反代 + 前端静态托管**：部署简单、证书易管；前端 Vite+React+TanStack Router/Query 是当下主流且正确的选择。
- **WebSocket 原生实现 + 单 worker 进程内广播**：实时刷新轻量落地；多 worker 时只需切 Redis pub/sub（接口不变），扩展路径清晰。
- **`chat_json` 结构化输出 + presigned URL + 公开地址签名 pin region**：都是工程上"踩过坑后修对"的细节，质量在线。

### 真实技术债务 / 漂移（需处理，按优先级）

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| P0 | **Celery 未真正下线，且与文档矛盾** | `docker-compose.yml` 仍定义 `celery-worker`/`celery-beat` 并 `Up`；日志显示 `generate_weekly_report_task` 经 `ForkPoolWorker` 成功执行。但 `PROGRESS.md`(08-19) 写"Celery worker 容器不在部署里、已改同步调用"。 | 确认后端是否仍 `send_task` 派发周报；若是，要么保留 Celery 并把它写进文档/架构图，要么彻底移除 beat 调度与 worker 容器。现状是"代码说不用、compose 还在跑、且真在跑任务"——**必须对齐**，否则排障时极易误判。 |
| P1 | **前端构建忽略 TypeScript 类型** | `build.js` 仅 `vite build`（esbuild 不去类型），`tsc --noEmit` 存在大量历史 number→string、`asChild` 错误被静默放过。 | 至少把 `npm run build` 改为 `tsc && vite build`（或单独 `typecheck` 纳入 CI 门禁），否则类型错误累积成隐性 bug（此前正是字段名错位 422/404 的根因之一）。 |
| P1 | **`.env` 明文 DeepSeek key 需确认未入库** | 根目录 `.env` 含 `LLM_API_KEY=sk-...`；需确认 `.gitignore` 覆盖 `.env`，否则密钥随仓库泄露（你此前在 logisticsAI 仓库已踩过 Gemini key 泄露的坑）。 | 确认 `.gitignore` 含 `.env`；若已提交，立即轮换 key + 清理历史。密钥改放 Docker secret / 环境变量注入。 |
| P2 | **compose 默认 `LLM_PROVIDER=mock`** | 不带 `.env` 启动则 AI 静默走 mock。 | 在 README/AGENTS 明确"AI 默认 mock，需 `.env` 开 cloud"；或把 `mock` 仅作单测用，dev 默认报错提醒。 |
| P2 | **`version: '3.9'` 已废弃** | `docker compose` 启动警告 `the attribute version is obsolete`。 | 删掉 `version` 字段。 |
| P3 | **文档漂移（不止 Celery）** | `PROGRESS.md` 说"ai-service 无源码卷挂载，改代码必须 build"，但 `docker-compose.yml:127` 实际 `./backend/ai_service:/app` 已挂载。 | 以 compose 与代码为真相源，定期回写 harness 文档；或脚本化校验文档与 compose 一致。 |

### 其他观察
- **后端镜像"年龄"与源码热更新**：backend / ai-service 均 `./backend:/app` 卷挂载 + `--reload`，源码改动热生效；但 `frontend` 是构建后由 Caddy 托管，**前端改动必须 `docker compose up -d --build frontend`** 才生效（此约束已写进 AGENTS.md，正确）。
- **无 CI 自动化测试**：现有 `e2e_all.py` / `e2e_ai.py` / `e2e_smoke.sh` 是宝贵的真机验证资产（已覆盖 14 功能 + 5 AI 功能，全 PASS），但靠人工跑；建议接 GitHub Actions，每次 PR 自动跑 e2e，防止"字段错位"类回归复现。
- **API 契约纪律**：早前前端大规模字段/路径错位（422/404）已逐条修通；根本解是用 OpenAPI 生成前端 client（如 openapi-typescript），让契约编译期校验，而非人工对齐。

---

## 五、总体结论

- **业务侧**：建模贴合修船行业、核心链路闭环且已端到端跑通，AI 定位务实（HITL + 规则打底 + RAG/LLM 增强），整体**合理且落地**。主要短板在权限粒度与 AI 降级 UX。
- **技术侧**：选型**务实、轻量、可自托管**，符合小团队；pgvector / MinIO / 原生 WS / 单 worker 广播都是聪明取舍。但 **Celery 漂移、前端类型债务、密钥管理** 三项是当前最该收口的技术风险，建议按 P0→P1 顺序处理。
- **一句话**：系统已经"能跑、跑通、业务对"，现在进入"收口技术债、补权限与 CI"的成熟期，而不是继续堆功能。

### 建议优先级
1. **立即**：核对并清理 Celery（对齐文档与 compose），确认 `.env` 已 gitignore。
2. **本周**：前端 `tsc` 纳入构建/CI 门禁；补 OpenAPI client 生成防契约回归。
3. **近期**：细粒度 RBAC；AI 降级时前端明确提示；e2e 接 CI 自动跑。
