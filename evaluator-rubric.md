# evaluator-rubric.md — 评审评分表

> 一轮（或几轮）会话后，按六个维度打分，每维度 0-2 分。
> **Evaluator 需要校准**：开箱即用的 agent 评审很弱（发现问题后说服自己通过）。见文末校准流程。

## 六个维度

| 维度 | 含义 |
|---|---|
| 1. 正确性 | 实现行为是否符合目标功能 |
| 2. 验证 | 要求的检查是否真跑过并留证据 |
| 3. 范围纪律 | 是否基本保持在选定功能范围内 |
| 4. 可靠性 | 重启 / 重跑后结果是否仍工作 |
| 5. 可维护性 | 代码与文档是否清楚到可交给下一轮 |
| 6. 交接准备度 | 新会话是否仅靠仓库内文件即可推进 |

每个维度：**0**（不达标）/ **1**（基本达标，有瑕疵）/ **2**（达标）。

## 结论选项

- **Accept** — 达标。
- **Revise** — 需修补才能接受。
- **Block** — 有根本性问题，需先解决。

## 本项目针对性通过/失败标准（建议，可随校准细化）

- **正确性**：端点返回 2xx 且字段对齐 `api_field_audit.md` / 前端需求；无复现已修回归（如任务创建路径、日报照片双前缀）。
- **验证**：后端改动有 `curl`/Postman 实测证据；前端改动有构建通过或浏览器实测证据。**空口「应该能跑」= 0 分**。
- **范围纪律**：未改动跨模块通用能力（MinIO/ai_client/JWT/pgvector）除非必要；一次只一个 `in_progress` 功能。
- **可靠性**：前端改动已 `docker compose up -d --build frontend`；backend 热更新已确认。
- **可维护性**：关键约束已写入 `AGENTS.md` / `PROGRESS.md`；新增功能已入 `feature_list.json`。
- **交接准备度**：`PROGRESS.md` 会话记录完整；`session-handoff.md` 在长会话后更新。

## 校准流程（预计 3-5 轮）

1. 用本 rubric 给一个已完成 sprint 打分。
2. 把分数与你自己的人工判断对比。
3. 有分歧处，把 rubric 的通过/失败标准写更具体（如上表）。
4. 对同一输出重跑 evaluator，看是否对齐。
5. 重复直到 evaluator 判断与人工评审基本一致。每轮记录改了什么、为什么。
