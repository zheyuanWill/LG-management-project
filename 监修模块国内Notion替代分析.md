# 监修模块 · 国内类 Notion 产品替代可行性分析

> 分析日期：2026-08-17
> 目的：判断「监修（Supervision）模块」的众多功能，是否有**国内、无需翻墙、可直接落地**的类 Notion 产品可以拿来直接实现。
> 信息来源：WalkingLabs《Learn Harness Engineering》工程化文档（已落地到本仓库 `AGENTS.md` 等）；飞书/语雀/明道云/Teable 等公开资料与 2026 年评测（检索于 2026-08-17）。

---

## 一、先给结论

**不能「一键替换」整个监修模块，但其约 70–80% 的终端用户功能（结构化数据、文档、风险、报表、知识问答、多人协作）可以用国内类 Notion / 多维表格产品低成本直接搭出来；剩下 20–30% 的「跨模块状态机 + 自定义存储 + 紧耦合角色 + 离线优先」属于集成/定制逻辑，需要额外低代码开发或保留自研后端。**

- ❌ **Notion 本体不能用**：国内访问慢、需翻墙、本土化短板明显（公开评测一致结论）。
- ✅ **首选国内替代：飞书多维表格 + 飞书文档 + 飞书知识问答**（无需翻墙、等保三级、支持私有化、2026 年 TableBench 榜单第一、已内置 AI agent 工作流节点）。
- ✅ **备选**：语雀（知识库向）、明道云 HAP（复杂流程 + 私有化 + 等保三级）、Teable（开源 + SQL 直连 + 私有化，适合保留现有数据栈）。
- ⚠️ **我来（wolai）**：最接近 Notion 的国内产品，但本次检索未能确认其当前运营活跃度，落地前务必先验证是否仍可注册/服务。

---

## 二、可用于直接实现的国内候选（无需翻墙）

| 产品 | 类型 | 核心能力 | 合规/部署 | 适用监修场景 |
|---|---|---|---|---|
| **飞书多维表格**（Lark Base） | 数据库型（Airtable/Notion-DB 等价） | 表格/看板/甘特/日历/表单视图；公式；自动化；OpenAPI/Webhook；行/字段级权限；仪表盘；AI（含工作流 AI agent 节点） | 等保三级；**支持私有化**；国内服务器 | 项目/任务/风险/报表数据层、自动化规则、实时协作 |
| **飞书文档** | Block 文档 | 结构化文档、模板、AI（豆包/DeepSeek/GPT）续写与摘要 | 同上 | 日报/周报模板、确认流转 |
| **飞书知识问答 / Aily** | 企业知识 AI | 吃内部文档后问答、可接业务系统 MCP | 同上 | 知识库 RAG 问答的近似替代 |
| **语雀** | 结构化知识库 | 目录式知识库、Markdown、AI（DeepSeek R1） | 等保三级 | 偏知识沉淀，日报/周报素材 |
| **明道云 HAP** | 零代码 aPaaS | 工作表/视图/工作流引擎/角色权限/外部门户；AI（Mingo）/MCP；**私有部署** | 等保三级、CMMI3、信创 | 复杂业务流程（含状态机）、需数据自主 |
| **Teable** | 开源多维表格 | 数据库内核、**SQL 直连**、百万行、私有化、信创 | 开源 + 私有化 | 想保留现有 SQL/MinIO 数据栈时的最佳桥梁 |
| **简道云 / WPS 智能表格** | 零代码表单 / 轻量表格 | 表单+流程 / WPS 内智能表 | 等保（简道云） | 轻量登记类，能力弱于飞书多维表 |

---

## 三、监修模块逐功能映射

> 监修模块组成见 `监修模块组件流程.md` 与 `功能与流程总览.md` 模块 3/7/8/9。

| # | 监修功能 | 能否直接用国内产品实现 | 落地方式 | 残留难点 |
|---|---|---|---|---|
| 1 | 监修项目主表/详情（船名、船东、状态、起止日期、编号 `LG-SV-`） | ✅ 直接 | 飞书多维表格建「监修项目」表；自动编号字段生成 `LG-SV-`；双向关联客户表；详情用「记录详情」页 | 无 |
| 2 | 任务管理（CRUD、状态流转、每日更新、历史版本） | ✅ 直接 | 任务表双向关联项目；看板视图按状态拖拽；每日更新用子表/时间线；历史由表格版本历史 + 记录留痕天然支持 | 无 |
| 3 | 日报 / 周报（模板文档 + AI 辅助生成 + 人在回路确认） | ✅ 直接（文档）+ ⚠️（AI 精度） | 飞书文档模板；飞书 AI 基于表格数据起草；确认=文档审批/评论 | 「基于现场数据 + 知识库 RAG 事实支撑」的精度弱于自研 RAG，取决于喂入数据质量 |
| 4 | 风险登记 + 规则引擎检测（阈值规则） | ✅ 直接 | 风险表 + 飞书多维表格「自动化」/公式实现阈值（未开始>50%→warning 等）；2026 已支持工作流 AI agent 节点 | 规则表达需在平台内重配，非直接搬代码 |
| 5 | 完工资料（文件归档） | ✅ 直接 | 附件字段 + 飞书云空间 | 失去自定义桶/生命周期控制 |
| 6 | 文件中心（统一文档库、presigned URL、MinIO） | ⚠️ 部分 | 飞书附件/云空间可替代 MinIO 存储；若需保留 MinIO，用 **Teable（SQL 直连 + 私有化）** 或飞书 API 接入 | 自定义签名/桶/生命周期不可控；需 API 桥接 |
| 7 | 知识库 RAG 问答 | ⚠️ 近似 | 飞书知识问答 / Aily 或语雀 AI 吃内部文档后问答 | 引用可追溯性、向量可控性不如自研 pgvector RAG |
| 8 | 随手存（现场文字/图片 + AI 联想活跃项目） | ⚠️ 部分 | 飞书文档/妙记快速记录 + AI agent 关联活跃项目 | 「OCR + 关键词匹配活跃项目打分」需飞书 AI agent 节点或自定义，非开箱 |
| 9 | **移交监修状态机**（brokerage_repair→supervision + 自动播种 5 任务） | ❌ 不能直接 | 需用飞书「自动化」/ 明道云工作流 / API 自研实现跨模块状态流转 | 这是定制业务流程，属低代码开发而非「拿来即用」 |
| 10 | 角色鉴权（admin/user/disabled、JWT） | ⚠️ 近似 | 飞书/明道云自带组织架构 + RBAC 行/字段级权限 | 现有自定义角色语义需映射，非完全照搬 |
| 11 | 实时推送（WebSocket） | ✅ 反而更好 | 飞书/多维表格原生多人实时协作，省去自研 WS（`lib/websocket.ts` 当前是死代码） | 无（本就是缺口） |

### 映射小结

- **可直接落地（≈70–80%）**：项目/任务/风险/报表数据层、文档模板、文件归档、多人实时协作、阈值规则自动化、知识问答近似。
- **需低代码/定制（≈20–30%）**：跨模块状态机（移交监修）、与现有 FastAPI/MinIO/Celery 的深度集成、紧耦合自定义角色、离线优先的船厂网络场景。

---

## 四、推荐方案（按诉求分三档）

**A. 最快见效、接受数据上云、团队已在飞书生态**
→ 直接用 **飞书多维表格 + 飞书文档 + 飞书知识问答**。监修的「数据 + 文档 + 风险 + 报表 + 知识 + 协作」一周内可搭出可用版本；移交状态机用多维表格自动化补齐。

**B. 要求数据自主 / 私有化 / 信创，且流程较复杂**
→ **明道云 HAP**（工作流引擎 + 角色权限 + 私有部署 + 等保三级），或用 **Teable**（开源、SQL 直连、私有化）接管数据层并把现有 MinIO/PostgreSQL 通过 API/SQL 接回。相当于用低代码重做后端，但保留数据主权。

**C. 想最小改动、保留现有自研栈**
→ 不做替换。监修模块当前后端（FastAPI + pgvector + MinIO + Celery）已实现核心事务逻辑；所谓「用 Notion 实现」只适用于前端协作层，不应动摇数据/集成底座。可把飞书文档仅作为日报/周报的协作编辑前端，后端仍走自研 API。

---

## 五、风险与注意事项

1. **Notion 本体不可用**：需翻墙，排除。
2. **我来（wolai）状态存疑**：最像 Notion 的国内品，但本次检索未确认其当前运营活跃度，落地前必须验证。
3. **数据主权**：飞书/语雀为公有云 SaaS，敏感修船业务数据上云需评估合规性；要自主请选明道云/Teable 私有化。
4. **AI 质量落差**：自研 RAG（pgvector + 知识库）的「引用可追溯 + 事实支撑」在通用知识问答产品上不可控，不能假设等同。
5. **迁移锁定**：低代码平台数据导出格式不通用（明道云等），长期存在供应商锁定风险；Teable 因 SQL 直连 + 开源最不易锁定。
6. **与现有栈的接缝**：若只替换前端协作层而保留 FastAPI/MinIO，需写 API 桥接（飞书开放平台 / Teable API），仍是工程投入，不是「零成本」。

---

## 六、一句话给决策者

> 监修模块**前台协作与数据呈现**那一大半，国内（飞书多维表格等）确实能免翻墙直接搭；但**后台的流程编排、存储签名、角色与离线**那小半，没有「拿来即用」的替代品——要么接受低代码重做，要么保留你们已有的 Docker 自研栈。别指望一个国内 Notion 能把整套 FastAPI+MinIO+Celery 替掉。
