# 监修（Supervision）模块 — 组件与流程详解

> 本文档聚焦「监修」业务线（项目类型 `supervision`）前端全部功能组件的**结构、props、调用的 API、以及用户操作流程**。
> 后端对应的 router 为 `projects`（监修主表）、`tasks`、`reports`（日报/周报）、`risks`；文件/知识库/随手存为跨模块通用能力。

---

## 一、路由与页面入口

| 路径 | 文件 | 作用 |
|---|---|---|
| `/supervision` | `src/routes/supervision/index.tsx` | 监修项目列表（可按状态/船名搜索） |
| `/supervision/$id` | `src/routes/supervision/$id.tsx` | 项目详情页（核心，承载所有子功能） |
| `/supervision/$id/daily/$date` | `src/routes/supervision/$id/daily.$date.tsx` | 单日日报独立页（从列表点开查看/确认） |

侧边栏 `Sidebar.tsx` 中「监修」项指向 `/supervision`；`TopBar.tsx` 把该路由标题映射为「监修」。

---

## 二、组件树（从列表到叶子）

```
SupervisionIndex (列表)
└─ 点击某行 → 跳转 /supervision/$id
      │
      SupervisionDetail ($id.tsx)            ← 详情页：统一拉数据 + 渲染头部 + Tabs
      ├─ 头部信息区
      │   ├─ ProjectHeader / 项目基本信息（船名、船东、状态、起止日期）
      │   ├─ 项目统计卡（GET /projects/{id}/stats：任务完成率、风险数等）
      │   └─ RiskTicker（顶部风险滚动条，红点提示）
      └─ ProjectTabs（5 个 Tab，均为 lazy 加载）
          ├─ 任务管理  → TaskList
          │     └─ TaskItem（每行）
          │           ├─ 状态下拉（PATCH /tasks/{id}）
          │           ├─ 删除（DELETE /tasks/{id}）
          │           ├─ DailyUpdateForm（填写今日更新 弹窗）
          │           └─ DailyUpdateHistory（历史记录 弹窗）★本次新增
          │                 └─ PhotoUploader（照片选择，maxPhotos=2）
          ├─ 日报      → DailyReportList
          │     ├─ 「生成日报」按钮（POST .../daily-reports/generate → 轮询 latest）
          │     ├─ 卡片列表（查看 / 确认 / 下载）
          │     └─ 点击 → /supervision/$id/daily/$date
          ├─ 周报      → WeeklyReportList
          │     ├─ 「生成周报」按钮（POST .../weekly-reports/generate → 轮询 latest）★上次新增
          │     └─ 卡片列表（查看 / 下载）
          ├─ 风险历史  → RiskList
          │     ├─ 「自动进度检查」按钮（POST .../risks/ai-detect，规则引擎）★已改名
          │     └─ 风险事件列表
          └─ 完工资料  → ProjectCompletion
                └─ 完工单/验收单等文件上传与展示
```

---

## 三、逐个组件说明

### 1. `routes/supervision/index.tsx` — 监修项目列表
- **数据**：`GET /projects?type=supervision`（实际由 `ProjectsPage` 通用列表按类型过滤）。
- **交互**：搜索框（船名/项目号）、查看按钮跳详情、新建按钮跳 `/projects/new?type=supervision`。
- **注意**：返回的是 `ProjectResponse` 基础字段，**不**带 `customerName`/`riskSummary`，红点由详情页另取。

### 2. `routes/supervision/$id.tsx` — 项目详情页（核心）
- **数据（并行拉取，挂载时一次性获取）**：
  - `GET /projects/{id}` → 项目主体
  - `GET /projects/{id}/stats` → 完成率/风险统计
  - `GET /tasks/projects/{id}/tasks` → 任务列表
  - `GET /reports/projects/{id}/daily-reports/latest`（或列表）→ 日报
  - `GET /reports/projects/{id}/weekly-reports/latest` → 周报
  - `GET /risks/projects/{id}/risks` → 风险
- **刷新机制**：子组件任何写操作后回调 `handleDataChange()`，但当前实现仅 bump 本地 `dataVersion`，`useApiGet` 的 queryKey 不含该变量 → **子 Tab 内数据需组件各自再拉**（例如 DailyReportList 内部再调 `useApiGet`）。这是已知刷新不完全的接缝，后续可统一用 `queryClient.invalidateQueries`。
- **布局**：头部（项目信息 + stats 卡 + `RiskTicker`）＋ `ProjectTabs`。

### 3. `components/project/ProjectTabs.tsx` — Tab 容器
- 5 个 Tab（任务管理 / 日报 / 周报 / 风险历史 / 完工资料），全部 `lazy()` 加载，带 `LoadingFallback`。
- 仅把从详情页取到的 `tasks/dailyReports/weeklyReports/risks` 作为 props 下传，**首次渲染用详情页缓存数据，后续写操作由各自组件内重新请求**。

### 4. `components/task/TaskList.tsx` — 任务管理
- **props**：`projectId, tasks, onTasksChange`
- **新建**：`POST /tasks/projects/{projectId}/tasks`，body `{name, status, priority, due_date, description}`（★已修正 URL，旧写法是 `/projects/${id}/tasks` 路径错误）。
- **渲染**：搜索 + 表格，每行用 `TaskItem`；创建成功回 `onTasksChange()`。

### 5. `components/task/TaskItem.tsx` — 任务行
- **props**：`task, onUpdate, onDelete`
- **就地操作**：
  - 状态下拉：`PATCH /tasks/{task.id}`（★`useApiPatch` 已修正为 `/tasks/{id}`）
  - 删除：`DELETE /tasks/{task.id}`
  - 「填写今日更新」→ 打开 `DailyUpdateForm`
  - 「历史记录」→ 打开 `DailyUpdateHistory` ★本次新增

### 6. `components/task/DailyUpdateForm.tsx` — 填写今日更新
- **props**：`task, date, open, onOpenChange, onSubmitted`
- **提交**：`POST /tasks/{task.id}/daily-updates`，body `{task_id, status, remark, update_date, photos, audio_duration}`（★已修正：旧代码是 `/tasks/tasks/{id}/daily-updates` 双前缀导致 **404**，现已去掉重复段）。
- **照片**：`PhotoUploader` 选择（每任务每天最多 2 张），提交时随表单上送 `photos[]`。
- **已知缺口**：后端 `create_daily_update` 只落 `task_daily_updates` 记录，**并未消费 `photos` 字段**，也未调用 `/daily-updates/{update_id}/photos` 上传接口 → 照片当前实际不会持久化（仅前端本地预览）。历史记录里也看不到照片。属待修数据接线问题。

### 7. `components/task/DailyUpdateHistory.tsx` — 历史更新记录 ★本次新增
- **props**：`task, open, onOpenChange`
- **数据**：`GET /tasks/{task.id}/daily-updates`（`enabled: open` 懒加载），按 `update_date` 倒序返回数组。
- **展示**：每条显示 日期、`status` 徽章（复用 `TASK_STATUS_LABELS`）、`remark` 原文、语音备注时长。空态提示「暂无历史更新记录」。
- **满足需求**：用户可在任务行点「历史记录」查看该任务**每天的状态/备注版本**（即你要的“每个任务的历史版本”）。

### 8. `components/task/PhotoUploader.tsx` — 照片选择器
- 纯前端组件：`accept="image/*"`，本地 `URL.createObjectURL` 预览，达到 `maxPhotos` 禁用；删除走 `onDelete(photoId)`。本身不发请求（上传动作由父组件编排，见第 6 点缺口）。

### 9. `components/report/DailyReportList.tsx` — 日报
- **props**：`projectId, reports, onReportsChange`
- **生成**：「生成日报」→ `POST /reports/projects/{id}/daily-reports/generate`（后端双并行调用 AI：明日计划 + 风险提示，并接入知识库 RAG），返回 `task_id` 后轮询 `.../daily-reports/latest` 出现新报告即刷新列表。
- **卡片操作**：查看（跳 `/supervision/$id/daily/$date`）、确认（`PATCH` 状态）、下载（指向 MinIO 预签名 URL）。

### 10. `components/report/WeeklyReportList.tsx` — 周报 ★上次新增
- 结构与日报一致：「生成周报」→ `POST .../weekly-reports/generate` → 轮询 `.../weekly-reports/latest`。
- 旧版周报 Tab 仅只读展示、无生成入口，现已补齐，与日报体验对齐。

### 11. `components/risk/RiskList.tsx` — 风险历史
- **props**：`risks, projectId`
- **「自动进度检查」按钮** ★已改名（旧「自动检测风险」）：`POST /risks/projects/{id}/risks/ai-detect`。
  - 后端是**规则引擎**（未开始>50%→warning；逾期→critical；完成率<30%→info），**非 LLM/AI**。代码里 toast 已写明“规则引擎”，仅按钮文案曾误导，现已更正。
- 列表展示风险等级、描述、关联任务、时间。

### 12. `components/risk/RiskTicker.tsx` — 顶部风险滚动条
- 详情页头部红点/滚动提示，点击可锚定到风险 Tab。数据来自详情页 `risks` prop。

### 13. `components/project/ProjectCompletion.tsx` — 完工资料
- 完工单 / 验收单等文件上传与展示（走 MinIO，文件 key 直接存，不进 files 表，见下方“文件中心”说明）。
- 这是修船完工阶段的资料归档视图。

---

## 四、端到端用户流程（典型）

1. **进入**：侧边栏「监修」→ `/supervision` 列表 → 点项目进入 `/supervision/$id`。
2. **概览**：详情页头部看项目信息 + 统计卡 + 风险红点。
3. **任务推进**：切「任务管理」Tab →
   - 新建任务（`POST /tasks/projects/{id}/tasks`）；
   - 每天点「填写今日更新」提交当日状态/备注（★不再 404）；
   - 点「历史记录」回看该任务所有日期的更新（★新增）。
4. **报表**：切「日报 / 周报」Tab → 点「生成日报 / 生成周报」触发 AI 辅助生成 → 人工确认 → 可下载。
5. **风险**：切「风险历史」Tab → 点「自动进度检查」按规则引擎登记风险。
6. **完工**：切「完工资料」Tab 归档完工/验收文件。

---

## 五、本轮已修 / 已知接缝（与本次排查相关）

| 项 | 状态 |
|---|---|
| `POST /tasks/{id}/daily-updates` 前端双前缀 → 404 | ✅ 已修（`DailyUpdateForm`） |
| `POST /tasks/projects/{id}/tasks` 路径错误 | ✅ 已修（`TaskList`） |
| 任务历史版本不可见 | ✅ 新增 `DailyUpdateHistory` |
| 文件下载/预览 `SignatureDoesNotMatch` | ✅ 后端改用公开地址签名（`get_minio_public_client`） |
| 每日更新照片实际未持久化（后端忽略 photos） | ⚠️ 待修（数据接线缺口，非本次范围） |
| 子 Tab 写操作后详情页缓存不自动刷新 | ⚠️ 接缝，建议统一 `invalidateQueries` |

> 注：下载修复仅改后端（`file_service.py` / `dependencies.py`），后端为卷挂载 + `--reload`，已热更新，无需重建镜像；前端两处改动需重建 frontend 镜像后生效。
