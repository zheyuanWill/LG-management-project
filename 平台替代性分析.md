# LG 船舶修理管理系统 — 平台替代性分析

> 目的：判断整个工程的哪些模块可以「数据搬到其它平台、我们平台只做集成」，哪些必须自研保留。
> 分析基准：逐模块对照 `功能与流程总览.md`（已对照源码核实）+ 现有 9 容器技术栈。
> 日期：2026-08-17

---

## 一、判断框架（怎么算「可替代」）

一个模块**可替代（数据外迁、平台集成）**，当且仅当它满足：

1. **数据是通用结构**（表格 / 表单 / 主从关系），不是专属算法产物；
2. **没有深度定制业务规则**（状态机、级联规则、阈值引擎）；
3. **不硬依赖我们的 AI 服务 / MinIO 签名网关 / 船厂离线约束**；
4. **价值在于「采集 + 协作 + 展示」**——而这恰恰是飞书多维表格 / 明道云 / Teable / 语雀的强项。

一个模块**不可替（必须自研）**，当它具有以下任一特征：

- 自研编排 / 状态机 / 级联生效逻辑；
- 强耦合我们的 AI 服务（embeddings / chat / OCR / 信息抽取）；
- 强耦合基础设施（presigned URL 网关、pgvector 向量检索）；
- 合规 / 离线约束（船厂弱网、信创私有化、数据不出域）。

> 核心结论：**本系统约 60% 的「数据形态」可外迁到类 Notion 平台；但承载业务逻辑 / AI / 基础设施 glue 的约 40% 必须保留在我们后端，作为「集成中枢」。**

---

## 二、模块级分类矩阵

图例：✅ 可替代（数据外迁）｜⚠️ 部分替代（数据可迁、逻辑/AI 保留）｜❌ 不可替（核心自研）

| # | 模块 | 当前实现要点 | 可替性 | 候选承接平台 | 外迁后我们平台仍要做什么 |
|---|---|---|---|---|---|
| 1 | 认证 Auth | JWT + 角色(user/admin/disabled) | ❌ 不可替（信任边界） | 企微/飞书 SSO（仅作登录源） | 保留鉴权中间件；可改用飞书/企微扫码登录，但角色与权限映射仍自管 |
| 2 | 客户 Customers | 船东主数据 CRUD + 模糊搜索 + 分页 | ✅ 可替代 | 飞书多维表格 / 明道云 / Teable | 通过开放 API/Webhook 同步主数据，供经纪/修船引用 |
| 3 | 监修 Supervision | 项目生命周期 + **移交监修状态机**（brokerage_repair→supervision 自动播种 5 任务） | ⚠️ 部分 | 多维表格（项目卡/进度） | **状态机 + 自动播种逻辑必须保留**；项目数据可迁，状态流转自管 |
| 4 | 买卖经纪 Brokerage-Sale | 4 张固定子表（survey/commercial/contract+MOA文件） | ✅ 可替代 | 多维表格 / 明道云（主从表） | 文件走统一存储网关；handover 标记简单可迁 |
| 5 | 修船经纪 Brokerage-Repair | 主表 + 调研/商务 + **经纪移交(handover)标志** | ✅ 可替代 | 同模块 4 | handover 布尔标志可在平台自动化实现；仍由我们触发移交 |
| 6 | 备件供应 Spare-Parts | 备件明细 + **物流 8 节点级联完成/重开规则** + HK签收 + 发票 | ⚠️ 部分 | 多维表格（数据）/ 明道云（自动化级联） | **级联生效规则是核心差异点**，建议保留自研；数据可迁 |
| 7 | 任务 Tasks | 任务 CRUD + 每日更新 + **每任务每天≤2 张照片限制** | ⚠️ 部分 | 多维表格（任务/日报） | 照片 2 张限制可平台自动化或保留；任务数据可迁 |
| 8 | 报表 Reports | 日报/周报 **AI 辅助 HITL**（propose→确认→finalize，RAG 事实支撑） | ⚠️ 部分 | 飞书文档（编辑）/ 飞书 AI | **AI 生成 + RAG 取数必须保留**；报表正文可存平台 |
| 9 | 风险 Risks | **确定性阈值规则引擎**（未开始>50% / 逾期 / 完成率<30%）+ 红点 | ⚠️ 部分 | 多维表格公式 / 明道云工作流 | 规则简单可在平台重建，但**红点联动日报/看板**自管更稳 |
| 10 | 随手存 Quick-Save | 现场记 + **AI 信息抽取 + 项目联想打分** | ❌ 不可替 | 语雀/飞书文档（仅存文本） | **AI 抽取 + 联想是我们的差异化能力**，必须保留 |
| 11 | 知识库 Knowledge | **RAG 向量检索**（pgvector + bge-small 512 + 流式 SSE） | ⚠️ 部分 | 飞书知识问答 / 钉钉知识库（替 Q&A） | 领域入库 pipeline + embeddings + 流式检索保留；纯问答可外迁 |
| 12 | 文件中心 Files | MinIO 对象存储 + **1h presigned 统一网关 `/files/url`** | ⚠️ 部分 | 阿里云OSS / 腾讯云COS / 七牛（替存储后端） | **统一签名网关是集成 glue**，保留薄网关、换后端即可 |

### 共享基础设施

| 设施 | 可替性 | 说明 |
|---|---|---|
| PostgreSQL + pgvector | ⚠️ 部分 | Teable 直接用 PG+SQL；但 pgvector 向量列不可替，除非迁 RAG 平台 |
| Redis + Celery | ❌ 不可替（编排 backbone） | 异步任务桥接（run_async 持久事件循环）；平台自动化仅替简单定时，复杂编排自管 |
| AI 服务（deepseek-chat / bge / OCR） | ❌ 不可替 | 全系统 AI 能力来源，Quick-Save/Reports/Knowledge/Risks 都依赖 |
| MinIO | ⚠️ 部分 | 对象存储是商品，可换 OSS/COS；但 presigned 网关模式保留 |
| Caddy 反代 / 前端 Vite | — | 集成中枢的入口，保留 |

---

## 三、逐模块判断理由（精简）

**✅ 纯数据、可整模块外迁**
- **Customers**：就是一张船东主数据表，搜索+分页+CRUD，无任何定制规则。多维表格开个表即可，我们靠 API 拉取做外键引用。
- **Brokerage-Sale / Brokerage-Repair**：买卖/修船经纪是「一个项目挂几张固定子表 + 上传合同文件」。本质是带附件的表单集合，主从结构所有低代码平台都原生支持；移交(handover)只是个布尔标志，平台自动化就能做。

**⚠️ 数据可迁，但「逻辑 / AI」必须留**
- **Supervision（监修主线）**：项目卡片、进度、标签页这些「壳」可迁；但「移交监修」状态机（类型切换 + 自动播种 5 条默认任务）是定制编排，平台没有现成对应，硬搬要重写工作流——不如留在后端。
- **Spare-Parts**：备件明细、照片、签收、发票都是数据，可迁；但物流 8 节点「完成 N 把前面全标记完成、重开 N 把后面全清空」的**级联规则**是这个模块的价值内核，是定制业务规则，建议在平台用自动化重建或保留自研。
- **Tasks**：任务与日报是标准表格；照片「每任务每天≤2 张」限制是轻量约束，平台自动化能拦，但保留更简单。
- **Reports**：日报/周报*内容*可以存飞书文档；但「AI 出草稿 → 人确认 → 定稿」的 HITL 流程依赖我们的 `ai_client` + 知识库 RAG 取事实，这部分是差异化，必须保留。
- **Risks**：所谓「AI 检测」实为阈值规则引擎，逻辑本身简单（百分比/逾期），理论上能在多维表格公式或明道云工作流重建；但红点横幅要跟日报、看板联动，留在后端联动更稳。
- **Knowledge**：向量入库 + pgvector 检索 + 流式 SSE 是自研 AI 基建；但「企业知识问答」这层飞书知识问答/钉钉知识库已经能替。建议：纯问答外迁，领域入库 pipeline 保留。
- **Files**：对象存储是商品，MinIO 可换 OSS/COS；但我们那套「所有 MinIO key 走 `/files/url` 统一 presigned」的网关是粘合各模块的 glue，必须保留一个薄网关。

**❌ 核心自研、不能外迁**
- **Auth**：身份是信任边界，哪怕改用飞书/企微 SSO 登录，角色与权限映射仍要自管。
- **Quick-Save**：它的存在意义就是「AI 现场抽取 + 联想关联项目」，抽掉 AI 就只剩个记事本——这是我们的差异化能力，必须保留。
- **AI 服务本身**：deepseek-chat / bge  embeddings / OCR 是全系统智能来源，无任何通用平台能直接替代（除非整块迁到飞书/钉钉的 AI 套件，但那就等于换基座）。
- **Celery/Redis 编排**：异步任务桥接（尤其是 `run_async` 持久事件循环保护 SQLAlchemy 连接池）是后端稳定性关键，平台自动化只替得了简单定时。

---

## 四、推荐目标架构：「我们平台 = 集成中枢」

```
┌─────────────────────────────────────────────────────────┐
│  外部平台（数据 + 协作层，无需翻墙）                        │
│  飞书多维表格 / 明道云 HAP / Teable / 语雀                  │
│  承接：客户、经纪、监修项目卡、任务、备件、报表正文、知识问答 │
└───────────────▲───────────────────────┬──────────────────┘
                │ 开放 API / Webhook      │ 开放 API / Webhook
                │ （数据双向同步）         │
┌───────────────┴───────────────────────▼──────────────────┐
│  LG 后端（保留，作为「集成中枢 + 逻辑/AI 引擎」）            │
│  · 鉴权 & 角色权限（信任边界）                              │
│  · 状态机 / 级联规则 / 阈值引擎（定制业务规则）              │
│  · AI 服务编排（抽取/联想/生成/RAG embeddings）             │
│  · 统一文件 presigned 网关（集成 glue）                     │
│  · Celery/Redis 异步编排 backbone                          │
└─────────────────────────────────────────────────────────┘
```

**落地优先级（按「外迁收益 / 改造风险」排序）：**
1. **先迁纯数据层**：Customers + 两个 Brokerage 子表 → 多维表格/明道云，风险最低、见效最快。
2. **再迁「数据 + 轻规则」**：Tasks、Spare-Parts 数据、Reports 正文 → 平台表格 + 自动化重建级联/照片限制。
3. **保留并强化中枢**：Auth、状态机、Quick-Save AI、Knowledge 入库、Files 网关、Celery 编排。
4. **可选外迁**：Knowledge 纯问答 → 飞书知识问答；MinIO → OSS/COS（仅换存储后端）。

---

## 五、结论一句话

> **能用其它平台代替的**：客户主数据、买卖/修船经纪表单、任务与日报数据、备件与物流数据、报表正文、知识库纯问答、对象存储后端——这些「数据形态」外迁后我们靠 API/Webhook 集成即可。
> **不能代替、必须自研的**：鉴权与角色、监修移交状态机、备件级联规则、风险阈值引擎、随手存 AI 抽取联想、知识库向量入库与流式检索、AI 服务本身、Celery 异步编排、统一文件签名网关——这些是业务逻辑、AI 能力与基础设施 glue，外迁要么丢失差异化，要么要重写工作流，不如留在我们后端当「集成中枢」。
