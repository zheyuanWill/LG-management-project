{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build:packages && pnpm --filter web-admin build",
  "outputDirectory": "apps/web-admin/dist"
}
