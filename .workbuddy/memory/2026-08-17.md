# 2026-08-17 工作日志

## harness 工程化（WalkingLabs 模板落地）
- 按 https://walkinglabs.github.io/learn-harness-engineering/zh/ 中文模板，给 LG 船舶管理 ERP 补齐全套 harness 文档：
  - 根指令：`AGENTS.md`、`CLAUDE.md`
  - 启动/验证：`init.sh`（Linux/Mac；Windows 用既有 `start-dev.bat`）
  - 进度：`PROGRESS.md`
  - 功能清单（机器可读）：`feature_list.json`
  - 支撑：`session-handoff.md`、`clean-state-checklist.md`、`evaluator-rubric.md`、`quality-document.md`
- 内容基于真实技术栈（FastAPI+pgvector+Redis+Celery+MinIO+AI 服务；Vite/React/Caddy）与既有业务文档，非套模板空壳。
- `feature_list.json` 现状：daily-update-photo-persist 为 blocked（后端忽略 photos）；supervision-refresh/brokerage-404-ux/knowledge-chat-persist/realtime-websocket 为 not_started。

## 监修模块国内 Notion 替代分析
- 产出 `监修模块国内Notion替代分析.md`：结论=不能一键替换整套，但 ~70-80% 终端功能（数据/文档/风险/报表/知识/协作）可直接用国内产品（首选飞书多维表格+文档+知识问答；备选语雀/明道云/Teable）落地；状态机/自定义存储/角色/离线那 20-30% 需低代码定制或保留自研。Notion 本体需翻墙排除；我来(wolai) 运营状态未确认需先验证。

## 全工程平台替代性分析（扩展版）
- 用户要求把分析从「监修模块」扩到「整个工程」，按「数据外迁、我们平台只做集成」vs「必须自研」二分。
- 产出 `平台替代性分析.md`：12 模块 + 共享基础设施逐条分类矩阵（✅可替/⚠️部分/❌不可替）。
- 结论：~60%「数据形态」可外迁（客户主数据、两个经纪子表、任务/日报数据、备件物流数据、报表正文、知识库纯问答、对象存储后端）；~40% 必须保留后端当代「集成中枢」（Auth/角色、监修移交状态机、备件级联规则、风险阈值引擎、Quick-Save AI 抽取联想、Knowledge 向量入库+流式、AI 服务本身、Celery 编排、Files presigned 网关）。
- 推荐目标架构：外部平台(飞书多维表格/明道云/Teable/语雀) 承接数据+协作层，LG 后端保留为集成中枢+逻辑/AI 引擎；落地优先级=先迁纯数据(Customers+经纪)→再迁数据+轻规则(Tasks/Spare/Reports)→保留强化中枢→可选外迁(Knowledge问答/MinIO后端)。

## 修理接线缺陷（本轮）
- 复核并修复此前审计中仍真实存在的接线缺陷；其余历史错配（/knowledge/ask、risk_reminders、tax_no、handed_over_at、survey_detail、item_name、node_type 等）经 grep 确认已在 2026-08-14 轮修掉，无需重改。
- **Fix 1（已确认在盘）**：`frontend/src/components/task/TaskList.tsx` 创建任务发 `due_date`→改为 `planned_end_date`（匹配后端 `TaskCreate`），计划结束日期不再静默丢失。
- **Fix 2（本轮落地，原 blocked 项 daily-update-photo-persist）**：
  - 前端 `DailyUpdateForm.tsx`：保留真实 `File`，创建每日更新后逐个 `POST /tasks/daily-updates/{id}/photos`（multipart）上传 MinIO；失败不阻断主流程。
  - 后端 `schemas/task.py`：`TaskDailyUpdateResponse` 增加 `photos` 字段。
  - 后端 `routers/tasks.py`：`create/list_daily_updates` 用 `joinedload` 预载 photos；新增 `GET /tasks/task-photos/{photo_id}/url` 返回 presigned URL。
  - 前端 `DailyUpdateHistory.tsx`：新增 `TaskPhotoThumb`，历史渲染持久化照片。
  - 验证：`py_compile` OK；`tsc --noEmit` 无新增错误（仅历史 number→string 噪音，Vite 构建忽略）。**本地栈未运行，live e2e 未跑**。
- **误报排除**：审计称 `customer.email`「静默丢失」——通读 model/schema/表单确认 email 字段根本未实现（非错配），不修。
- 同步更新 `feature_list.json`(daily-update-photo-persist: blocked→passing) 与 `PROGRESS.md`。

## e2e 真机验证（Docker Desktop 已开）
- 用户确认 Docker 已开，跑真实 e2e。栈：`docker compose up -d postgres redis minio backend frontend ai-service`；**关键：后端代码烘焙进镜像（非 bind-mount），每次改 backend 需 `docker compose build backend` 再 `up -d backend`，仅 restart 无效。**
- 默认管理员 `admin/admin123`（`seed_admin_user` 注入）；API 前缀 `/api/v1/{auth,projects,tasks,...}`。
- 写回归脚本 `e2e_smoke.sh`（login→建 supervision 项目→建任务带 planned_end_date→建每日更新→传真实 PNG→列表 round-trip→取 presigned URL）。**ALL CHECKS PASSED**：Fix1 planned_end_date 持久化、Fix2 照片全链路（上传 storage_key→列表命中→预览 URL）均验证。
- **e2e 暴露并修复 2 个后端缺陷**：
  1. `joinedload(photos)` 后 `.scalar_one()`/`.scalars().all()` 必须加 `.unique()`，否则 SQLAlchemy `The unique() method must be invoked...` 500（create 改 `db.refresh(update,["photos"])`；list 改 `result.unique().scalars().all()`）。
  2. `dependencies.get_minio_public_client` 未 pin region，`presigned_get_object` 在容器内解析 bucket region 连 `localhost:9000` 失败（容器内不可达 MinIO）→ 500；加 `region="us-east-1"`（MinIO 默认，与 make_bucket 一致）后纯本地签名、URL host 仍保留公开地址。
- 测试数据(项目3-7/任务1-5/照片1-3)已 DELETE 清理，dev 库无残留。

## 修复剩余 4 个 not_started 功能 + 全量 e2e（本轮，用户硬要求每个 live e2e 通过才停）
- 4 项全部 implemented + live e2e PASS：`supervision-refresh`、`brokerage-404-ux`、`knowledge-chat-persist`、`realtime-websocket`（详见 `PROGRESS.md` 本轮会话块 + `feature_list.json` 已置 passing）。
- 关键改动：
  - `supervision-refresh`：删 `setDataVersion` 死状态，改 `invalidateQueries` 详情页真实 queryKey。
  - `brokerage-404-ux`：brokerage 4 GET + spare_parts hk-signatures/invoices GET 改 `Optional[...]`+`return None` → 200+null。
  - `knowledge-chat-persist`：新增 `KnowledgeChatMessage` 模型 + `/knowledge/chat-messages`(GET/POST/DELETE)；`QAChat.tsx` 后端优先、localStorage 兜底。
  - `realtime-websocket`：新增 `app/ws.py`（原生 FastAPI WS，按 project_id 分房间，token 鉴权）；`main.py` 挂 `/ws/projects/{project_id}`；`create_daily_update` 触发 `broadcast`；**Dockerfile 改单 worker**（多 worker 破坏进程内广播，多 worker 需 Redis pub/sub）；`lib/websocket.ts` 改原生 WS（原 socket.io 死代码）；`$id.tsx` 订阅房间 → 刷新+提示。
- **顺带修潜在缺陷**：`project_service.generate_project_number` 原 `COUNT+1` 撞唯一约束 500 → 改 `MAX(已有序号)+1`。
- 新回归脚本 `e2e_all.py`（urllib + websockets，零 mock）：4 功能全绿。`tsc` 改动文件 0 新增错误；`npx vite build` 通过（1855 模块）。**注意**：`npm run build` 在本沙箱因 safe-delete 对 `dist` 的 trash 步骤失败（环境限制，非代码），用 `npx vite build` 替代验证。
- e2e 用 managed venv `C:/Users/zheyu/.workbuddy/binaries/python/envs/default` 的 `websockets` 包（已 pip 安装）。
- 测试项目(含失败轮次残留的 20 个 E2E 项目)已 DELETE 清理。
